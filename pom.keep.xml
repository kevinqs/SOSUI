<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.xtime</groupId>
  <artifactId>dp-extjs</artifactId>
  <packaging>jar</packaging>
  <version>1.2-SNAPSHOT</version>
  <name>dp7 extjs Maven jar</name>
  <url>http://maven.apache.org</url>
    <parent>
        <groupId>com.xtime</groupId>
        <artifactId>xsencha-base</artifactId>
        <version>1.1-SNAPSHOT</version>
    </parent>
    <dependencies>
        <dependency>
            <groupId>com.sun.jersey</groupId>
            <artifactId>jersey-server</artifactId>
            <version>1.8</version>
        </dependency>
    </dependencies>

    <!-- We will be using 7.4 sencha commons -->
    <properties>
        <sencha.commons.version>7.4-SNAPSHOT</sencha.commons.version>
    </properties>
  <profiles>
        <profile>
            <id>sencha-app-build</id>
             <activation>
                <file>
                    <exists>build.properties</exists>
                </file>
            </activation>
            <build>
                <plugins>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-war-plugin</artifactId>
			             <executions>
							 <execution>
	           					 <phase>none</phase>
	       					 </execution>
       					 </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>1.7</version>

                        <executions>

                            <execution>
                                <id>build-jar-folder-structure</id>
                                <phase>prepare-package</phase>

                                <goals>
                                    <goal>run</goal>
                                </goals>

                                <configuration>
                                    <target>
                                    	<move  todir="${project.build.directory}/${project.build.finalName}/META-INF/resources/extjs" includeEmptyDirs="true">
                                            <fileset dir="${project.build.directory}/${project.build.finalName}"/>
                                        </move>
                                   </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    
                    <plugin>
				        <groupId>org.apache.maven.plugins</groupId>
				        <artifactId>maven-jar-plugin</artifactId>
				        <version>2.4</version>
				        <configuration>
						  <classesDirectory>${project.build.directory}/${project.build.finalName}</classesDirectory>
				        </configuration>
			      </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>1.7</version>

                        <executions>

                            <execution>
                                <id>production files rename</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>

                                <configuration>
                                    <target>
                                        <echo>Splitting CSS file</echo>
                                        <exec executable="ruby" dir="${basedir}/lib">
                                            <arg value="split_css.rb"/>
                                            <arg value="${project.basedir}/target/${project.build.finalName}/META-INF/resources/extjs/resources"/>
                                        </exec>


                                        <!--<move file="${project.basedir}/target/${project.build.finalName}/resources/DP-all.css" tofile="${project.basedir}/target/${project.build.finalName}/resources/CP3-all.css" failonerror="false"/>-->
                                        <!--<move file="${project.basedir}/target/${project.build.finalName}/resources/DP-example.css" tofile="${project.basedir}/target/${project.build.finalName}/resources/CP3-all_2.css" failonerror="false"/>-->
                                        <!-- Just for debugging -->
                                        <!--<exec executable="ruby" dir="${basedir}/lib">-->
                                        <!--<arg value="count_css_rules.rb"/>-->
                                        <!--<arg value="${project.basedir}/target/${project.build.finalName}/resources"/>-->
                                        <!--</exec>-->
                                    </target>
                                </configuration>
                            </execution>

                        </executions>

                    </plugin>
                  

                </plugins>

                </build>
                
        </profile>

    </profiles>
    
  <build>
    <finalName>dp-extjs</finalName>
  </build>
</project>
